export enum AutomationNodeKind {
  Trigger = 'trigger',
  Action = 'action',
  Condition = 'condition',
  Delay = 'delay',
  End = 'end',
  LoopBack = 'loopBack',
  Placeholder = 'placeholder',
  AddStep = 'addStep',
}

export enum AutomationFileKind {
  Automation = 'automation',
}
